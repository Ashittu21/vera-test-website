#Options All
#Order deny,allow
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /louisiana-locked-up/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>